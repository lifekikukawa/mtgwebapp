<script type="text/javascript">

console.log("読み込み成功");

$.getJSON("/AllSets-x.json", function(json){
  console.log(json);
  console.log(json.BFZ);
  var test =  Object.keys(json.BFZ.cards).length;;
  console.log(test);
  console.log(escape(json.BFZ.cards[2].name));

  //$("img").attr("src",ret);
  for (var i = test - 1; i >= 0; i--) {
    var name = json.BFZ.cards[i].name;
    name = name.replace(/,\s/g,'_');
    name = name.replace(/\s/g,'+');
    name = name.replace(/'/g,'_');
    name = name.replace(/,/g,'_');
    console.log(name);

    ret = "http://mtg-jp.com/cardlist/thumbs/BFZ_t/"+name+'.jpg';
    $("table").append(
    "<tr>"+
      "<td>"+
        "<img src="+ret+">"+
      "</td>"+
    "</tr>"
    );
  };


});

</script>

<table>

</table>

